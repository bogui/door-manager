# Copy this file to secrets.yaml and fill in real values.
# secrets.yaml is gitignored — never commit it.
# Home Assistant loads this automatically — no restart needed after changes (reload via Developer Tools → YAML → Reload secrets).

# Full GDS3710 HTTP API URL for opening the south door.
# Replace the password at the end with your actual PANEL_PASSWORD.
open_south_door_url: "http://192.168.1.20/goform/apicmd?cmd=open_door&user=admin&pwd=changeme"
